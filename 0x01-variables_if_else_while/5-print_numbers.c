#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0 to 9
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	printf("\n"0);
	return (0);
}

